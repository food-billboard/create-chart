export type TRateConfig = {
  backgroundColor: ComponentData.TColorConfig;
  rateBackgroundColor: ComponentData.TColorConfig;
  size: number;
  count: number;
  margin: number;
  defaultValue: number;
  shape: string;
  allowHalf: boolean;
  allowClear: boolean;
};
