import {} from 'react';
import {} from 'antd';
import { connect } from 'dva';
import { mapStateToProps, mapDispatchToProps } from './connect';

const AbsorbGuideLine = () => {
  return <div></div>;
};

export default connect(mapStateToProps, mapDispatchToProps)(AbsorbGuideLine);
