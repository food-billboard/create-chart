import React from 'react';
import classnames from 'classnames';
import styles from './index.less';

const Painter = () => {
  return (
    <div className={classnames(styles['page-design-main-panel'], 'pos-re')}>
      painter
    </div>
  );
};

export default Painter;
